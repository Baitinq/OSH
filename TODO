python env? rlm
computer use? (screenshot, move mouse, press key?)
auto-compact
session persistance?
cli mode so it can spawn itself?
knowledge about itself in order to customize itself if needed? or not
scratchpad/notes? for agent to not miss things?
slash summarize? when u loose context of what happened and slash model and slash reasoning
git-first?
other-agent aware (e.g. if other agents are working maybe it can somehow know idk)
hardware awareness? where is it running on (specs, os)
/tree
scrathpad/progress for human to see what is going on
dump state to file as saving (binary but then hjard to read for agents, so maybe something else)
